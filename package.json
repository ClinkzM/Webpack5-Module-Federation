{
  "name": "webpack5-module-federation",
  "private": true,
  "workspaces": {
    "packages": [
      "appA",
      "appB"
    ]
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
